# Usa l'immagine ufficiale di Bun
FROM oven/bun

# Imposta la working directory dentro il container
WORKDIR /app

# Copia tutti i file del progetto nel container
COPY . .

# Installa le dipendenze con Bun
RUN bun install

# (Opzionale) Costruisce il frontend se necessario
RUN bun run build

# Espone la porta usata dal tuo server Bun
EXPOSE 8080

# Avvia il server Bun
CMD ["bun", "src/server/index.ts"]
